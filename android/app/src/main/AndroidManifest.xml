<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Weekly flash card notifications (instructions.md section 36).
         Deliberately no SCHEDULE_EXACT_ALARM/USE_EXACT_ALARM or
         USE_FULL_SCREEN_INTENT — notifications are scheduled inexactly
         (a review nudge, not an alarm) and never take over the
         lockscreen, so neither special permission is needed. -->
    <!-- Required for all outbound network calls (AI provider APIs). Without
         this, sockets are refused at the OS layer before any request is
         sent — Dio surfaces that as a connection error, which reads to the
         user as "You're offline" even on a fully connected device. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Auto-backup is deliberately ON: it's the only data-loss protection
         for learner progress (the whole Drift database lives inside the
         backed-up app_flutter/ directory). The res/xml rules below carve
         out only the AI provider API keys stored by flutter_secure_storage
         — their encryption is bound to this device's Android Keystore, so a
         restore onto different hardware would be unreadable anyway (and the
         app treats a failed decrypt as "no key entered", which is safe). -->
    <application
        android:label="Teacher"
        android:name="${applicationName}"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Lets flutter_local_notifications reschedule weekly flash card
             notifications after a reboot (they're stored in-plugin, not
             persisted by the OS across a restart otherwise). -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
