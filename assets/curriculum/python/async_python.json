{
  "learningPathId": "python",
  "moduleId": "async-python",
  "title": "Async and Concurrency",
  "order": 14,
  "concepts": [
    {
      "id": "python-async-python",
      "title": "Async and Concurrency",
      "description": "Concurrency and parallelism are different things; the GIL means threads don't give standard Python true CPU parallelism; and async/await is a single-threaded event loop that helps enormously with I/O-bound waiting but does nothing at all for CPU-bound work.",
      "moduleId": "async-python",
      "topicId": "async-python",
      "difficulty": "advanced",
      "estimatedMinutes": 50,
      "prerequisites": ["python-functions-basics"],
      "learningObjectives": [
        "Distinguish concurrency (interleaving many tasks) from parallelism (running tasks literally at the same time).",
        "Explain what the GIL is and why it prevents true CPU parallelism between threads in standard Python.",
        "Choose between threads, processes, and asyncio based on whether a workload is I/O-bound or CPU-bound.",
        "Explain async/await and the event loop's cooperative scheduling model without treating it as automatic parallelism.",
        "Predict why converting a CPU-bound function to async provides no speedup, and identify what would actually help."
      ],
      "explanation": {
        "sections": [
          {
            "heading": "Concurrency vs. Parallelism: Interleaving vs. Simultaneity",
            "body": "These two words get used interchangeably in casual conversation, but they describe genuinely different things, and the difference matters for everything that follows in this concept. **Concurrency** means making progress on multiple tasks over the same span of time, by interleaving them -- working a bit on task A, switching to task B while A is stuck waiting on something, coming back to A once it's ready, and so on. Nothing here requires more than one thing to be physically happening at the exact same instant; it just requires *not wasting time* while one task is idle. **Parallelism** means multiple tasks are genuinely, physically executing at the same literal moment, typically because there's more than one CPU core actually doing work simultaneously.\n\nA useful analogy: a single chef cooking three dishes by starting the rice, then while it simmers chopping vegetables for the second dish, then while those saute checking on a sauce for the third -- that's concurrency. One chef, interleaving, never doing two physical actions in the same instant, but still getting three dishes done in less total time than doing them one at a time, start to finish. Three separate chefs, each with their own stove, actually cooking three dishes at the same literal moment -- that's parallelism. Concurrency is about *structure* (not blocking on idle time); parallelism is about *hardware* (multiple execution units genuinely running at once). A program can be concurrent without being parallel, and this distinction is the key to understanding why asyncio helps with some problems and not others."
          },
          {
            "heading": "The GIL: Why Threads Don't Give You True CPU Parallelism",
            "body": "Standard Python (CPython, the reference implementation almost everyone uses) has a **Global Interpreter Lock** (GIL) -- a single lock that only one thread can hold at a time, and holding it is required to execute Python bytecode. In practice, this means that even if your program creates ten threads, only *one* of them is ever actually executing Python code at any given instant; the others are waiting their turn for the GIL, no matter how many CPU cores your machine has. The GIL exists mainly for implementation simplicity and safety -- it protects CPython's internal memory management (reference counting for garbage collection) from race conditions without needing much finer-grained, and much more complex, locking throughout the interpreter.\n\nThe practical consequence: for CPU-bound work -- code that's genuinely crunching numbers, transforming data, running tight loops -- Python threads do **not** give you a speedup from using multiple cores, because the GIL ensures only one thread's Python bytecode runs at a time regardless of core count. Adding more threads to a CPU-bound task doesn't parallelize the crunching; it just adds the overhead of switching the GIL back and forth between threads that are all fighting over the same single lock, which can occasionally make things *slower*, not faster. This is a genuinely distinctive property of CPython -- other languages (and other Python implementations, like Jython or specific GIL-free experimental builds) don't necessarily share this limitation -- but for the CPython most people run, it's a fact worth internalizing early, because it directly determines which concurrency tool is even capable of solving a given problem."
          },
          {
            "heading": "Threads vs. Processes vs. asyncio: Three Tools, Different Jobs",
            "body": "Given the GIL, Python offers three different concurrency tools, and picking the right one comes down to one question: **is the workload spending most of its time waiting on something external (I/O-bound), or spending most of its time actively computing (CPU-bound)?**\n\n- **Threads** (`threading`): multiple threads within a single process, sharing memory directly. Because of the GIL, threads don't help CPU-bound work, but they *do* help I/O-bound work -- when a thread is blocked waiting on a network response, a disk read, or similar, it releases the GIL while waiting, letting another thread run Python code in the meantime. Good fit: I/O-bound tasks where you specifically want or need blocking-style, traditional synchronous-looking code (e.g. using a library that isn't async-aware) run concurrently.\n- **Processes** (`multiprocessing`): multiple separate OS processes, each with its own Python interpreter and its own GIL -- meaning they genuinely can run on separate CPU cores at the same literal moment. This is the tool that actually achieves parallelism for CPU-bound work, at the cost of more overhead (starting a process is heavier than starting a thread) and no shared memory by default (data has to be explicitly serialized and passed between processes, which has its own cost). Good fit: CPU-bound work like heavy numeric computation, image or video processing, or anything genuinely limited by how fast the CPU can crunch through data.\n- **asyncio**: a single thread, running a single event loop, that cooperatively switches between many tasks whenever one of them is waiting on I/O. No threads, no processes, no GIL contention between multiple Python-executing threads at all -- just one thread, doing one thing at a time, but very efficiently avoiding ever sitting idle while something else could be making progress. Good fit: a very large number of concurrent I/O-bound operations (thousands of open network connections, for instance), where the overhead of one OS thread per task would be prohibitive, and where the libraries involved are async-aware.\n\nNone of these three is a strictly better default than the others -- they solve different shapes of problem, and reaching for the wrong one (threads for CPU-bound work, or asyncio wrapped around a workload that's actually CPU-bound) leaves you no better off than doing the work sequentially, sometimes worse once you count the added complexity."
          },
          {
            "heading": "async/await and the Event Loop: Cooperative Scheduling, Not Magic",
            "body": "`async def` defines a **coroutine function** -- calling it doesn't run its body immediately; it returns a coroutine object that has to be driven by something else (the event loop) to actually execute. `await` marks a point where a coroutine can pause and hand control back to the event loop, specifically at a spot where it's waiting on something (typically I/O) rather than doing active computation:\n\n```python\nimport asyncio\n\nasync def fetch_page(name, delay):\n    print(f\"Starting {name}\")\n    await asyncio.sleep(delay)   # simulates waiting on a network response\n    print(f\"Finished {name}\")\n    return f\"{name} result\"\n\nasync def main():\n    results = await asyncio.gather(\n        fetch_page(\"A\", 2),\n        fetch_page(\"B\", 1),\n    )\n    print(results)\n\nasyncio.run(main())\n```\n\nThe event loop is, at its core, just a single-threaded loop that keeps a list of tasks and repeatedly asks: \"which of my tasks are actually ready to make progress right now, versus which ones are currently just waiting on something external?\" When `fetch_page(\"A\", 2)` hits `await asyncio.sleep(2)`, it doesn't block the entire program for two seconds -- it tells the event loop \"I have nothing more to do until this two-second timer finishes; go run something else in the meantime.\" The event loop then switches to `fetch_page(\"B\", 1)`, runs it until *it* hits an await point, and keeps juggling between whichever tasks are actually ready. This is exactly why the output above prints \"Starting A\", \"Starting B\" nearly immediately (both begin, then both immediately hit their await and yield control), then \"Finished B\" around the 1-second mark, then \"Finished A\" around the 2-second mark, totaling roughly 2 seconds overall -- not 3, which is what running them one after another sequentially would have taken. Nothing here is running in parallel in the hardware sense; it's one thread, doing one thing at a time, just never sitting idle burning wall-clock time when it could be making progress on something else that's ready."
          },
          {
            "heading": "Why Making a CPU-Bound Function async Doesn't Help",
            "body": "This is one of the most common, genuinely understandable misconceptions once someone has seen asyncio speed up network-heavy code: \"I'll just make my slow, CPU-heavy function `async` and it'll run faster.\" It will not -- and understanding exactly why ties the whole concept together. The event loop's entire trick is *switching to other work while a task is waiting on something external*. A CPU-bound function -- one that's actively computing the whole time, with no I/O to wait on -- never hits an `await` point where it's genuinely idle; it just runs, using the CPU, from start to finish. Marking it `async def` and awaiting it doesn't change that at all: the event loop hands it control, it runs to completion doing pure computation with the GIL held throughout, and *only then* does it return control to the loop. Nothing else gets a chance to run during that entire stretch -- you've achieved zero overlap, because there was never any idle waiting time to overlap with in the first place. Worse, if this happens inside an app that's also handling other async tasks (like serving other users' requests), that long CPU-bound stretch actually blocks *everything else* in the event loop too, since it's all cooperatively sharing that one single thread -- something that would have been handled by a separate thread or process now stalls the entire application while it runs.\n\nThe fix is to reach for the tool that actually matches the workload: run CPU-bound work in a separate process (`multiprocessing`, or `asyncio`'s own `loop.run_in_executor()` combined with a `ProcessPoolExecutor`, which lets you kick off CPU-heavy work on another process while still cooperating nicely with an async event loop for everything else), rather than trying to make the event loop's I/O-waiting trick apply to work that was never waiting on anything to begin with."
          }
        ]
      },
      "examples": [
        {
          "title": "asyncio Helping Enormously: Many Concurrent Network Requests",
          "language": "python",
          "code": "import asyncio\nimport time\n\nasync def fetch(name, seconds):\n    await asyncio.sleep(seconds)   # stands in for an actual network call\n    return f\"{name} done\"\n\nasync def main():\n    start = time.perf_counter()\n    results = await asyncio.gather(\n        fetch(\"request-1\", 1),\n        fetch(\"request-2\", 1),\n        fetch(\"request-3\", 1),\n    )\n    elapsed = time.perf_counter() - start\n    print(results)\n    print(f\"Took {elapsed:.1f}s\")   # ~1.0s, not ~3.0s\n\nasyncio.run(main())",
          "explanation": "Each fetch() call simulates a network request that takes 1 second, spent entirely waiting rather than computing. asyncio.gather() starts all three coroutines and lets the event loop run them concurrently: when the first hits await asyncio.sleep(1), it yields control back to the loop instead of blocking the whole program, letting the second and third start their own waits almost immediately afterward. Because all three are waiting on independent one-second timers at roughly the same time, the total elapsed time is close to 1 second, not 3 -- the waiting overlapped rather than stacking up sequentially. This is the textbook case where asyncio shines: dozens, hundreds, or thousands of operations that spend most of their time waiting on something external (a real version of this would be actual HTTP requests to different servers) can all be in flight concurrently on a single thread, with no need for one OS thread per request."
        },
        {
          "title": "asyncio Doing Nothing for CPU-Bound Work",
          "language": "python",
          "code": "import asyncio\nimport time\n\ndef is_prime(n):\n    if n < 2:\n        return False\n    for i in range(2, int(n ** 0.5) + 1):\n        if n % i == 0:\n            return False\n    return True\n\nasync def count_primes_async(start, end):\n    # marking this 'async' changes nothing about how it executes --\n    # there is no await anywhere inside it, because there's nothing to wait on\n    return sum(1 for n in range(start, end) if is_prime(n))\n\nasync def main():\n    start_time = time.perf_counter()\n    results = await asyncio.gather(\n        count_primes_async(0, 200_000),\n        count_primes_async(200_000, 400_000),\n    )\n    elapsed = time.perf_counter() - start_time\n    print(results)\n    print(f\"Took {elapsed:.2f}s\")   # roughly the same as running both sequentially, back to back\n\nasyncio.run(main())",
          "explanation": "count_primes_async is declared async def, and it's awaited via asyncio.gather() exactly like the network example above -- but there is no await anywhere inside its body, because checking whether numbers are prime is pure CPU computation with nothing to wait on. When the event loop runs the first call, it executes start to finish without ever yielding control back, because it never hits a point where it has nothing to do but wait -- it always has more computing to do, right up until it returns a result. The second call then has to wait for the entire first call to finish before it even gets a turn, since the loop is single-threaded and the first call never released control. The total time ends up roughly equal to running both ranges one after another sequentially -- asyncio.gather() created the *appearance* of concurrency by starting both, but delivered none of the actual benefit, because the underlying workload never had any idle waiting time for the event loop to fill with other work. Fixing this for real speedup would mean running the two ranges in separate OS processes (e.g. via concurrent.futures.ProcessPoolExecutor), so they can execute on genuinely separate CPU cores at the same time -- true parallelism, which asyncio alone never provides."
        }
      ],
      "misconceptions": [
        {
          "description": "If I make my slow, CPU-heavy function async and await it, it'll run faster, the same way asyncio sped up my network request code.",
          "clarification": "asyncio's speedup comes specifically from filling otherwise-idle waiting time with other useful work -- when a coroutine hits await on something that's genuinely waiting (a network response, a timer, a disk read), the event loop switches to another task instead of sitting idle. A CPU-bound function has no idle waiting time at all; it's actively using the CPU from the moment it starts until the moment it finishes. Wrapping it in async def and awaiting it doesn't create any waiting time that wasn't there before -- the function still runs start to finish as one uninterrupted block, and while it runs, nothing else in the event loop gets a turn either, since everything shares that one single thread. The fix for a genuinely CPU-bound bottleneck is a separate process (true parallelism across CPU cores), not asyncio, which only ever helps with I/O-bound waiting."
        },
        {
          "description": "Python threads don't give real parallelism for CPU-bound work, so there's no point ever using threading in Python -- just use asyncio or multiprocessing for everything.",
          "clarification": "Threads remain genuinely useful for I/O-bound work, specifically because a thread that's blocked waiting on I/O (a network call, a file read, many blocking library calls) releases the GIL while it waits, letting other threads run Python code during that idle time -- this is real, valuable concurrency, even though it's not parallelism. Threads are also often the simplest option when you need to run traditional, blocking-style code concurrently (for instance, using a library that isn't written to work with asyncio's async/await style at all, and can't easily be changed) without rewriting everything around an event loop. The real, narrower rule is: don't expect threads to speed up CPU-bound work, because the GIL prevents that specific benefit -- not that threads are useless in general."
        }
      ],
      "exercises": [
        {
          "id": "python-async-python-ex1",
          "type": "predictOutput",
          "prompt": "What is printed, and in roughly what order, and about how long does main() take to run in total?",
          "code": "import asyncio\n\nasync def worker(name, delay):\n    print(f\"{name} starting\")\n    await asyncio.sleep(delay)\n    print(f\"{name} finished\")\n\nasync def main():\n    await asyncio.gather(\n        worker(\"A\", 3),\n        worker(\"B\", 1),\n    )\n\nasyncio.run(main())",
          "hints": [
            "Both worker() calls start running almost immediately when gather() launches them -- neither has to wait for the other to finish before it even begins.",
            "Each worker prints its 'starting' message, then immediately hits await asyncio.sleep(...) and yields control back to the event loop -- it doesn't block anything else while it waits.",
            "The two sleeps run concurrently, not back-to-back -- the total time is determined by whichever delay is longest, not by adding the delays together."
          ],
          "expectedAnswer": "'A starting' and 'B starting' print first, in that order (roughly together, since both begin almost immediately), then 'B finished' after about 1 second, then 'A finished' after about 3 seconds total. The whole thing takes roughly 3 seconds overall -- not 4 -- because the two sleeps overlap concurrently rather than running one after the other.",
          "solutionExplanation": "asyncio.gather() starts both worker() coroutines essentially together. Each one prints its 'starting' message first, then immediately hits await asyncio.sleep(delay), handing control back to the event loop rather than blocking. Since both are now just waiting on independent timers, the event loop has nothing else useful to interleave with here, but crucially the two timers are running concurrently, not sequentially -- B's 1-second timer finishes first, printing 'B finished' around the 1-second mark, and A's 3-second timer finishes later, printing 'A finished' around the 3-second mark. Total elapsed time for main() is roughly 3 seconds, governed by the longer of the two delays, rather than 4 seconds, which is what running worker(\"A\", 3) followed by worker(\"B\", 1) sequentially, one after the other, would have taken."
        },
        {
          "id": "python-async-python-ex2",
          "type": "coding",
          "prompt": "You've written an async web scraper that fetches many pages concurrently using aiohttp-style async requests, and it works great -- fast, efficient, handles hundreds of pages without spawning hundreds of threads. Now a teammate adds a step that runs after each page is fetched: a CPU-heavy text analysis function (parsing and scoring the page's text, taking about 200ms of pure computation per page) that has no I/O in it at all. They wrote it as `async def analyze(text): ...` with no `await` inside, and awaited it alongside the fetches, expecting it to stay fast. Explain what will actually happen to overall throughput, and rewrite the analysis step so it doesn't stall the rest of the scraper's concurrency.",
          "hints": [
            "Ask whether analyze() ever actually yields control back to the event loop -- does it contain any await pointing at something genuinely waiting on I/O?",
            "Because analyze() is pure computation with no await inside, once the event loop starts running it, nothing else -- including other pages' fetches that are ready to proceed -- gets a turn until it fully finishes.",
            "Look for a way to hand CPU-bound work off to something other than the event loop's single thread, while still being able to await its eventual result from within async code -- concurrent.futures has a tool for exactly this, paired with an event loop's executor support."
          ],
          "solutionCode": "import asyncio\nfrom concurrent.futures import ProcessPoolExecutor\n\ndef analyze_sync(text):\n    # the actual CPU-heavy work, as an ordinary (non-async) function\n    ...\n    return score\n\nasync def analyze(text, executor):\n    loop = asyncio.get_running_loop()\n    # runs analyze_sync in a separate process, and awaits the result\n    # without blocking the event loop's single thread while it runs\n    return await loop.run_in_executor(executor, analyze_sync, text)\n\nasync def process_page(url, executor):\n    text = await fetch_page(url)         # I/O-bound -- genuinely benefits from being awaited\n    score = await analyze(text, executor)  # CPU-bound -- now runs in another process, not inline\n    return score\n\nasync def main(urls):\n    with ProcessPoolExecutor() as executor:\n        return await asyncio.gather(*(process_page(url, executor) for url in urls))",
          "solutionExplanation": "Declaring analyze() as async def with no await inside it doesn't change how it executes at all -- it still runs as one uninterrupted, 200ms block of pure computation, and during that entire stretch, the single-threaded event loop cannot switch to anything else, including other pages' fetches that may already be sitting ready to proceed. With hundreds of pages, those 200ms blocks pile up serially on the one thread, one after another, stalling exactly the concurrency the scraper was built to get -- the CPU-bound step becomes a bottleneck that blocks everything else, not just itself. The fix hands the actual CPU-heavy work off to a ProcessPoolExecutor -- a pool of separate OS processes -- via loop.run_in_executor(), which schedules analyze_sync to run in another process (genuinely in parallel with the main event loop and with other worker processes, since each process has its own GIL) and returns an awaitable that the event loop CAN treat as a real waiting point, freeing it to keep servicing other pages' fetches while the analysis runs elsewhere. The I/O-bound fetch step stays exactly as it was, since asyncio already handles that well; only the genuinely CPU-bound step needed a different tool."
        }
      ],
      "assessments": [
        {
          "id": "python-async-python-assess-mcq1",
          "type": "multipleChoice",
          "prompt": "A Python program spawns four threads, each running a tight numeric loop that does heavy computation with no I/O at all, on a machine with four free CPU cores. What should you expect?",
          "options": [
            "Roughly a 4x speedup, since there are four threads and four free cores available.",
            "No meaningful speedup over running the same work in a single thread, because the GIL only allows one thread to execute Python bytecode at a time, regardless of how many cores are free.",
            "The program will raise a runtime error, since CPython does not allow multiple threads to run CPU-bound code at all.",
            "A speedup proportional to the number of cores, but only for numeric code specifically, since numeric operations are exempt from the GIL."
          ],
          "correctOptionIndex": 1,
          "explanation": "The GIL ensures that only one thread executes Python bytecode at any given instant, no matter how many CPU cores are idle and available -- so four threads doing CPU-bound work don't run four-way in parallel; they take turns on a single core's worth of actual execution, with some added overhead from switching the GIL between them. Option A describes what would happen with true parallelism (e.g. via separate processes), which threading does not provide for CPU-bound work in CPython. Option C is wrong -- this code runs fine, just without the parallel speedup someone might expect. Option D is a fabricated exemption; there is no general carve-out for numeric code specifically (certain C-extension libraries can release the GIL internally during specific operations, but that's a property of those specific libraries' implementation, not a blanket rule about 'numeric code')."
        },
        {
          "id": "python-async-python-assess-scenario1",
          "type": "scenario",
          "prompt": "You're building a tool that needs to do two things: (1) download and process metadata for 5,000 URLs from a remote API, and (2) run a computationally expensive image-resizing operation on 500 local image files. Explain which concurrency approach -- threading, multiprocessing, or asyncio -- fits each part best, and why using the same tool for both parts would be a mistake.",
          "modelAnswer": "The two parts have fundamentally different bottlenecks, so they call for different tools. Downloading metadata for 5,000 URLs is I/O-bound -- almost all the time is spent waiting on network responses, not computing anything -- which is exactly the shape of problem asyncio is built for: a single thread can have thousands of requests conceptually 'in flight' at once, switching to whichever one is ready to make progress whenever another is stuck waiting on the network, with far less overhead than spinning up 5,000 OS threads. Resizing 500 images, on the other hand, is CPU-bound -- the bottleneck is genuinely how fast the CPU can crunch through pixel data, with little to no waiting involved. asyncio would provide no benefit here at all, since there's no idle waiting time for an event loop to fill with other work; each resize would just run start to finish as one uninterrupted block, exactly as if it were called directly. Threading wouldn't help either, since the GIL means only one thread's Python bytecode runs at a time, so four threads each resizing images wouldn't run four resizes in parallel -- they'd just take turns on one core. multiprocessing is the right tool for this half: spreading the 500 images across several separate OS processes, each with its own GIL, genuinely lets multiple images be resized at the same literal moment on separate CPU cores. Using asyncio for the image resizing would leave that part just as slow as doing it sequentially (while adding unnecessary complexity), and using multiprocessing for 5,000 network requests would work but waste a large amount of memory and process-startup overhead for a problem that a single async event loop could handle far more efficiently -- picking the tool that matches each workload's actual bottleneck matters more than picking one tool and forcing both problems through it."
        },
        {
          "id": "python-async-python-assess-mcq2",
          "type": "multipleChoice",
          "prompt": "A single-core machine runs a program using asyncio to handle 100 concurrent network requests. Is this concurrency, parallelism, both, or neither?",
          "options": [
            "Concurrency only -- the requests are interleaved on a single thread while waiting on I/O, but nothing is literally executing at the same physical instant, which is what parallelism would require",
            "Parallelism only, since 100 requests are genuinely all 'in flight' at once",
            "Both concurrency and parallelism, since asyncio's event loop always uses multiple CPU cores internally to manage its scheduling",
            "Neither -- true concurrency requires at least two physical CPU cores to begin with"
          ],
          "correctOptionIndex": 0,
          "explanation": "Concurrency is about structure -- making progress on multiple tasks by interleaving them, never wasting time while one is idle -- and requires nothing about the underlying hardware. Parallelism specifically requires multiple things physically happening at the same literal instant, which a single core can never provide, since it can only execute one instruction at a time. asyncio's single-threaded event loop is a textbook example of concurrency without parallelism: many tasks make progress over the same span of time by interleaving whenever one is waiting, but at any given instant, only one is actually executing."
        },
        {
          "id": "python-async-python-assess-mcq3",
          "type": "multipleChoice",
          "prompt": "What is the primary reason CPython's GIL exists?",
          "options": [
            "To protect CPython's internal memory management (reference counting for garbage collection) from race conditions, without requiring much more complex, fine-grained locking throughout the interpreter",
            "To make Python code run faster on multi-core machines by automatically coordinating work across cores",
            "To prevent programmers from accidentally writing infinite loops in threaded code",
            "It's a requirement imposed by Python's open-source license terms"
          ],
          "correctOptionIndex": 0,
          "explanation": "The GIL exists mainly for implementation simplicity and safety: it protects CPython's internal reference-counting garbage collector from race conditions using one single, simple lock, rather than requiring much finer-grained locking scattered throughout the interpreter's internals. It has nothing to do with license terms (option D), doesn't prevent infinite loops (option C), and -- notably -- actively works against multi-core speedups for CPU-bound code rather than coordinating or enabling them (option B is exactly backwards)."
        },
        {
          "id": "python-async-python-assess-mcq4",
          "type": "multipleChoice",
          "prompt": "A program creates 10 threads, each making a blocking network request expected to take about 2 seconds, all starting at roughly the same time. Approximately how long should the whole batch take, and why?",
          "options": [
            "About 2 seconds total -- each thread releases the GIL while blocked waiting on its network response, letting the others proceed concurrently, so the waiting overlaps rather than stacking up",
            "About 20 seconds total, since the GIL forces the threads to run strictly one at a time even while they're blocked waiting on I/O",
            "About 2 seconds, but only because the GIL is entirely disabled for the whole program the moment any network operation begins",
            "It depends entirely on the number of CPU cores available, since each thread needs its own dedicated core to make any progress at all"
          ],
          "correctOptionIndex": 0,
          "explanation": "This is exactly the case where threads genuinely help despite the GIL: a thread blocked on I/O (waiting for a network response) releases the GIL for the duration of that wait, letting other threads run Python code -- or start their own blocking calls -- during that idle time. Since all 10 requests are waiting on their own independent ~2-second network responses at roughly the same time, that waiting overlaps, and the whole batch finishes in roughly 2 seconds, not 20. This doesn't require multiple CPU cores (option D) -- releasing the GIL during I/O wait is what makes the overlap possible, not simultaneous execution on separate cores."
        },
        {
          "id": "python-async-python-assess-mcq5",
          "type": "multipleChoice",
          "prompt": "Which of these is a genuine cost or tradeoff of using multiprocessing instead of threading, according to this lesson?",
          "options": [
            "Starting a new process carries more overhead than starting a new thread, and since processes don't share memory by default, data passed between them must be explicitly serialized, which has its own cost",
            "Processes are fundamentally unable to run on more than one CPU core at the same time, unlike threads",
            "multiprocessing is not part of the Python standard library and must be installed separately",
            "Processes all share the exact same GIL, so there's no actual benefit over threading for CPU-bound work"
          ],
          "correctOptionIndex": 0,
          "explanation": "Each process gets its own separate Python interpreter and its own GIL, and starting one is heavier than starting a thread, plus there's no shared memory by default -- any data that needs to move between processes has to be explicitly serialized and passed across, which costs real time and effort. This is genuinely the tradeoff multiprocessing accepts in exchange for true CPU parallelism. Options B and D describe the opposite of what actually happens (multiple GILs, one per process, is precisely what enables real parallelism), and multiprocessing is a standard library module, not a third-party dependency (option C)."
        },
        {
          "id": "python-async-python-assess-mcq6",
          "type": "multipleChoice",
          "prompt": "```python\nimport asyncio\n\nasync def worker(name, delay):\n    print(f\"{name} start\")\n    await asyncio.sleep(delay)\n    print(f\"{name} end\")\n\nasync def main():\n    await asyncio.gather(worker(\"X\", 2), worker(\"Y\", 2), worker(\"Z\", 1))\n\nasyncio.run(main())\n```\nWhat's the order of printed output, and roughly how long does main() take?",
          "options": [
            "X start / Y start / Z start / Z end / X end / Y end -- total time about 2 seconds",
            "X start / Y start / Z start / X end / Y end / Z end -- total time about 5 seconds",
            "Z start / Z end / X start / X end / Y start / Y end -- total time about 5 seconds",
            "X start / Y start / Z start / Z end / X end / Y end -- total time about 5 seconds"
          ],
          "correctOptionIndex": 0,
          "explanation": "asyncio.gather() starts all three coroutines together; each prints its 'start' message and immediately hits await asyncio.sleep(...), yielding control back to the event loop. All three sleeps then run concurrently rather than sequentially: Z's 1-second timer finishes first, printing 'Z end' around the 1-second mark, and X's and Y's 2-second timers finish together afterward, around the 2-second mark, in the order they were scheduled. The whole thing takes roughly 2 seconds total -- governed by the *longest* delay among concurrently-running tasks, not the sum of all three delays (which would be 5 seconds if they ran one after another sequentially)."
        },
        {
          "id": "python-async-python-assess-mcq7",
          "type": "multipleChoice",
          "prompt": "```python\nimport asyncio\n\nasync def greet():\n    print(\"hello\")\n\ncoro = greet()\nprint(\"created\")\n```\nWhat gets printed, in order?",
          "options": [
            "created (only) -- calling greet() returns a coroutine object without running any of its body; \"hello\" never prints because the coroutine is never awaited or otherwise driven by an event loop",
            "hello then created",
            "created then hello",
            "It raises an error immediately, since a coroutine object can't be created without an event loop already running"
          ],
          "correctOptionIndex": 0,
          "explanation": "Calling an async def function does not execute its body -- exactly like calling a generator function, it immediately returns an object (here, a coroutine object) that has to be driven by something else, specifically the event loop, in order to actually run. Since coro is never awaited, passed to asyncio.run(), or scheduled in any other way, the body of greet() -- and therefore \"hello\" -- never executes at all in this snippet. \"created\" prints normally, since that line runs regardless. Creating a coroutine object doesn't require an event loop to already be running; only actually driving it to completion does."
        },
        {
          "id": "python-async-python-assess-mcq8",
          "type": "multipleChoice",
          "prompt": "According to this lesson, what is the correct fix for a genuinely CPU-bound function that's currently slowing down an otherwise-fast asyncio application?",
          "options": [
            "Run the CPU-bound work in a separate process (e.g. via multiprocessing, or loop.run_in_executor with a ProcessPoolExecutor), so it executes in true parallel on another core instead of blocking the event loop's single thread",
            "Add extra await statements inside the CPU-bound function, even where there's nothing to actually wait on, so the event loop can interleave it more often",
            "Increase the number of internal threads the asyncio event loop itself uses",
            "Just await the function directly -- async def functions are automatically scheduled onto a separate CPU core by the interpreter"
          ],
          "correctOptionIndex": 0,
          "explanation": "Since the whole problem is that CPU-bound work has no idle waiting time for the event loop to fill with other tasks, the fix has to come from outside the event loop entirely: running the work in a separate OS process gives it a genuinely separate GIL and, potentially, a separate CPU core, achieving real parallelism instead of trying to force cooperative scheduling onto work that was never waiting on anything. Adding fake await points (option B) doesn't create any genuine idle time to exploit. The event loop is fundamentally single-threaded by design (option C misunderstands this), and async def alone never causes automatic multi-core scheduling (option D) -- that's precisely the misconception this lesson corrects."
        },
        {
          "id": "python-async-python-assess-mcq9",
          "type": "multipleChoice",
          "prompt": "Can a program achieve genuine parallelism on a machine with only one CPU core?",
          "options": [
            "No -- parallelism specifically requires multiple execution units (such as multiple cores) doing work at the exact same instant; a single core can only run one instruction at a time, so at most it can achieve concurrency, never true parallelism",
            "Yes, as long as the program creates enough threads",
            "Yes, as long as the program uses asyncio's event loop, since the event loop itself runs in parallel with the tasks it schedules",
            "Yes, but only specifically for I/O-bound workloads"
          ],
          "correctOptionIndex": 0,
          "explanation": "Parallelism is fundamentally a hardware property: it requires more than one execution unit genuinely doing work at the same literal moment. A single CPU core, no matter how many threads or coroutines a program creates on top of it, can only ever execute one instruction at a time -- more threads just mean more things taking turns, which is concurrency, not parallelism. Neither threading (option B) nor asyncio (option C) creates additional cores out of nothing, and this limitation is about the underlying hardware, not about whether the workload happens to be I/O-bound or CPU-bound (option D)."
        },
        {
          "id": "python-async-python-assess-mcq10",
          "type": "multipleChoice",
          "prompt": "A service needs to maintain roughly 10,000 simultaneous open network connections, each mostly idle, occasionally receiving a message. Which approach does this lesson describe as the best fit, and why?",
          "options": [
            "asyncio -- a single thread can efficiently track thousands of waiting tasks, whereas dedicating one OS thread to each connection would carry prohibitive overhead at that scale",
            "threading -- one dedicated thread per connection is always the simplest and most efficient choice, at any scale",
            "multiprocessing -- one process per connection ensures each connection gets genuine, dedicated CPU parallelism",
            "None of Python's concurrency tools can realistically handle more than a few hundred simultaneous connections"
          ],
          "correctOptionIndex": 0,
          "explanation": "This is exactly the scenario the lesson identifies as asyncio's strongest fit: a very large number of concurrent, mostly-idle I/O-bound operations, where the overhead of one OS thread (or, far worse, one OS process) per connection would be prohibitive at scale. A single event loop can track thousands of waiting connections efficiently, switching to whichever one actually has a message ready, without needing anywhere near 10,000 separate OS-level threads or processes -- each of which carries real memory and scheduling overhead that would make options B and C impractical at this scale."
        }
      ]
    }
  ]
}
